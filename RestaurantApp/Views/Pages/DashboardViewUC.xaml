<UserControl x:Class="RestaurantApp.Views.Pages.DashboardViewUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Foreground="Black"
             Loaded="UserControl_Loaded">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="{DynamicResource DashboardTitle}" Foreground="{DynamicResource TextPrimaryColor}" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource ExcelExportTitle}" Margin="0,0,20,0" Style="{StaticResource SecondaryButtonStyle}" Click="ExportClick"/>
                <Border Background="{DynamicResource BgColor}" 
                    BorderBrush="{DynamicResource BorderDefaultColor}" BorderThickness="1" 
                    CornerRadius="18" HorizontalAlignment="Right" Padding="2">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="{DynamicResource 1WeekPeriod}" Style="{StaticResource TimeFilterRadioButton}" Checked="Filter_Checked" Tag="7" IsChecked="True"/>
                        <RadioButton x:Name="Rb1Month" Content="{DynamicResource 1MonthPeriod}" Style="{StaticResource TimeFilterRadioButton}" Checked="Filter_Checked" Tag="30"/>
                        <RadioButton x:Name="Rb3Months" Content="{DynamicResource 3MonthPeriod}" Style="{StaticResource TimeFilterRadioButton}" Checked="Filter_Checked" Tag="90"/>
                        <RadioButton x:Name="Rb6Months" Content="{DynamicResource 6MonthPeriod}" Style="{StaticResource TimeFilterRadioButton}" Checked="Filter_Checked" Tag="180"/>
                        <RadioButton x:Name="Rb1Year" Content="{DynamicResource 1YearPeriod}" Style="{StaticResource TimeFilterRadioButton}" Checked="Filter_Checked" Tag="365"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="3" Margin="-10,0,-10,25">
            <Border Style="{StaticResource CardStyle}" >
                <StackPanel >
                    <TextBlock Text="{DynamicResource TotalRevenueTitle}" Foreground="{DynamicResource TextSecondaryColor}" FontSize="14" FontWeight="Medium"/>
                    <TextBlock x:Name="RevenueTB" Text="0 ₽" Foreground="{DynamicResource TextPrimaryColor}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="{DynamicResource OrdersCountTitle}" Foreground="{DynamicResource TextSecondaryColor}" FontSize="14" FontWeight="Medium"/>
                    <TextBlock x:Name="OrdersCountTB" Text="0" Foreground="{DynamicResource TextPrimaryColor}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="{DynamicResource AvgCheckTitle}" Foreground="{DynamicResource TextSecondaryColor}" FontSize="14" FontWeight="Medium"/>
                    <TextBlock x:Name="AvgCheckTB" Text="0 ₽" Foreground="{DynamicResource TextPrimaryColor}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="{DynamicResource SecondaryBgColor}" BorderBrush="{DynamicResource BorderDefaultColor}" BorderThickness="1" CornerRadius="12" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{DynamicResource RevenueTitle}" Foreground="{DynamicResource TextPrimaryColor}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <lvc:CartesianChart Grid.Row="1" Series="{Binding ChartSeries}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding ChartLabels}">
                            <lvc:Axis.Separator>
                                <lvc:Separator Step="1" StrokeThickness="1" Stroke="{DynamicResource BorderDefaultColor}"  StrokeDashArray="4"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis LabelFormatter="{Binding PriceFormatter}">
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{DynamicResource BorderDefaultColor}" StrokeDashArray="4"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</UserControl>
