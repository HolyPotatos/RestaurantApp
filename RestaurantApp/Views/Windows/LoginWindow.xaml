<Window x:Class="RestaurantApp.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource LoginTitle}" WindowStyle="None" Height="440" Width="380" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Background="Transparent" AllowsTransparency="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" 
                      ResizeBorderThickness="5" 
                      GlassFrameThickness="0" 
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Border Background="{DynamicResource BgColor}" CornerRadius="12" BorderBrush="{DynamicResource BorderDefaultColor}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{DynamicResource LoginTitle}" 
                       VerticalAlignment="Bottom" 
                       HorizontalAlignment="Left" 
                       FontSize="14" 
                       Margin="30,0,0,6" 
                       Foreground="{DynamicResource TextPrimaryColor}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0">
                <Button WindowChrome.IsHitTestVisibleInChrome="True" Style="{DynamicResource CloseButtonStyle}" Click="Close_Click"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Center" Grid.Row="1" Margin="30,0">
                <TextBlock Text="{DynamicResource AppTitle}" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="{DynamicResource TextPrimaryColor}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,5"/>

                <TextBlock Text="{DynamicResource LoginSubTitle}" 
                               FontSize="14" 
                               Foreground="{DynamicResource TextSecondaryColor}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,30"/>

                <TextBlock Text="{DynamicResource LoginTBTitle}" 
                               Foreground="{DynamicResource TextSecondaryColor}" 
                               Margin="0,0,0,5"/>
                <TextBox x:Name="LoginTextBox" 
                             Style="{DynamicResource TextBoxStyle}" 
                             Tag="{DynamicResource LoginTBMask}" 
                             Height="42" 
                             Margin="0,0,0,15"/>

                <TextBlock Text="{DynamicResource PasswordPBTitle}" 
                               Foreground="{DynamicResource TextSecondaryColor}" 
                               Margin="0,0,0,5"/>
                <Grid Height="42" Margin="0,0,0,10">
                    <PasswordBox x:Name="UserPasswordBox" 
                                     Style="{DynamicResource PasswordBoxStyle}"
                                     PasswordChanged="UserPasswordBox_PasswordChanged"
                                     GotFocus="UserPasswordBox_GotFocus"
                                     LostFocus="UserPasswordBox_LostFocus"/>

                    <TextBlock x:Name="PasswordPlaceholder"
                                   Text="{DynamicResource PasswordPBMask}"
                                   Foreground="{DynamicResource TextSecondaryColor}"
                                   VerticalAlignment="Center"
                                   FontSize="14"
                                   Margin="13,0,0,0"
                                   IsHitTestVisible="False"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.ColumnSpan="2" Margin="0,0,0,10">
                        <TextBlock FontSize="14" Visibility="Collapsed" x:Name="LoginErrorTB" Text="{DynamicResource EnterLoginError}" Foreground="{DynamicResource DangerColor}" HorizontalAlignment="Center"/>
                        <TextBlock FontSize="14" Visibility="Collapsed" x:Name="PasswordErrorTB" Text="{DynamicResource EnterPasswordError}" Foreground="{DynamicResource DangerColor}" HorizontalAlignment="Center"/>
                        <TextBlock FontSize="14" Visibility="Collapsed" x:Name="WrongErrorTB" Text="{DynamicResource WrongLoginOrPasswordError}" Foreground="{DynamicResource DangerColor}" HorizontalAlignment="Center"/>
                        <TextBlock FontSize="14" Visibility="Collapsed" x:Name="BlockErrorTB1" Text="{DynamicResource BlockAccountError1}" Foreground="{DynamicResource DangerColor}" HorizontalAlignment="Center"/>
                        <TextBlock FontSize="14" Visibility="Collapsed" x:Name="BlockErrorTB2" Text="{DynamicResource BlockAccountError2}" Foreground="{DynamicResource DangerColor}" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="LoginButton" Grid.Row="1" Grid.Column="1"
                            Content="{DynamicResource LoginBtn}"
                            Margin="5,0,0,0"
                            Style="{DynamicResource PrimaryButtonStyle}" 
                            Height="45"
                            Click="LoginButton_Click"/>

                    <Button Grid.Column="0"  Grid.Row="1"
                            Content="{DynamicResource CancelBtn}"
                            Margin="0,0,5,0"
                            Style="{DynamicResource SecondaryButtonStyle}" 
                            Height="45"
                            Click="Close_Click"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>
